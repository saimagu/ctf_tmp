<svg xmlns="http://www.w3.org/2000/svg" onload="
  fetch('/flag')
    .then(r => r.text())
    .then(f => {
      let el = document.createElement('div');
      el.innerText = f;
      el.style = 'font-size:2em;color:red';
      document.body.appendChild(el);
    });
"></svg>
